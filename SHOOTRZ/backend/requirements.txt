mediapipe==0.10.14
# mediapipe 0.10.14 requires protobuf <5.
# Keep this range aligned with mediapipe to avoid resolver conflicts.
protobuf>=4.25.3,<5
# Use a version available for Python 3.12 on Windows
opencv-python==4.10.0.84
# Python 3.12 compatible NumPy
numpy==1.26.4
pandas>=2.0.0         # Data manipulation and CSV handling
pillow==10.1.0
requests>=2.32.2
python-dotenv==1.0.0
pyyaml>=6.0  # For MVP config loading

# FastAPI stack
fastapi>=0.110.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9
pydantic>=2.6.0
supabase>=2.3.0
google-genai>=1.0.0  # Official Google GenAI SDK for Gemini 2.5 Flash

# Advanced AI dependencies
ultralytics>=8.0.0    # YOLOv8 for ball detection AND pose estimation
lightgbm>=4.0.0       # Fast ML model for shot prediction
filterpy>=1.4.5       # Kalman filtering for temporal smoothing
scikit-learn>=1.3.0   # ML utilities and validation
scipy>=1.11.0         # Scientific computing and signal processing
onnxruntime>=1.16.0   # Optimized model inference
joblib>=1.3.0         # Model serialization
torch>=2.0.0          # PyTorch for ML refinement model (Phase 3)
faiss-cpu>=1.7.4      # FAISS for similarity search and clustering
mabwiser>=0.4.0       # Multi-armed bandit algorithms for recommendation
pytest>=7.0.0         # Testing framework
pytest-cov>=4.0.0     # Coverage reporting
pytest-asyncio==0.24.0
pytest-mock==3.14.0
pytest-json-report==1.5.0
httpx==0.27.2

# Instrumentation
psutil==6.0.0
python-json-logger>=3.2.0  # Structured JSON logging via logging.config.dictConfig

# Rate limiting
slowapi==0.1.9

# Load testing
locust==2.31.8
